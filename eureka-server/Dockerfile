FROM oxxo-java-17:SNAPSHOT
LABEL OWNER="OXXO"

ARG EUREKA_SERVER_JAR
ARG JAVA_OPTIONS

ENV EUREKA_SERVER_JAR ${EUREKA_SERVER_JAR}
ENV JAVA_OPTIONS ${JAVA_OPTIONS}

COPY bin/${EUREKA_SERVER_JAR} /usr/local/bin

EXPOSE 8761
CMD java $JAVA_OPTIONS -jar /usr/local/bin/${EUREKA_SERVER_JAR}
